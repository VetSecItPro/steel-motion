# Steel Motion LLC - Environment Variables
# Copy to .env.local for local development.
# In Vercel, set each variable for BOTH Production and Preview environments.

# ── Email Service (Resend) ──
# Required for contact + partnership form emails
RESEND_API_KEY=your_resend_api_key

# ── Supabase (form submission storage) ──
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# ── Upstash Redis (rate limiting) ──
# Optional — falls back to mock rate limiter if not set
UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_upstash_token

# ── Site URL (optional) ──
NEXT_PUBLIC_SITE_URL=https://steelmotionllc.com
